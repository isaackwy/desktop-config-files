--- a/dmenu.c	2022-10-12 02:26:23.201874762 -0700
+++ b/dmenu.c	2022-10-12 02:31:42.424862754 -0700
@@ -174,7 +174,7 @@
 			drawitem(item, x, y += bh, mw - x);
 	} else if (matches) {
 		/* draw horizontal list */
-		x += inputw;
+		x += inputw / 1.25;
 		w = TEXTW("<");
 		if (curr->left) {
 			drw_setscheme(drw, scheme[SchemeNorm]);
